# 🎯 Шаблон разработки выставочных тач-панелей (Touch Panel Template)

> **⚠️ ВАЖНО: Это REFERENCE документ, а не строгая спецификация!**
> 
> Данный документ описывает **подход, паттерны и архитектурные решения**, которые успешно применяются в этом проекте. Используй его как:
> - 📖 **Справочник** - посмотри, как реализованы типовые задачи
> - 🧭 **Ориентир** - следуй этим паттернам для консистентности
> - 💡 **Источник идей** - адаптируй решения под конкретные требования
> 
> **НО**: всегда выполняй **конкретные задачи пользователя/разработчика**, а не слепо копируй шаблон. Если требования отличаются - адаптируй подход под задачу!

## 📋 Оглавление
1. [Назначение документа](#назначение-документа)
2. [Технологический стек](#технологический-стек)
3. [Архитектура приложения](#архитектура-приложения)
4. [Ключевые концепции и паттерны](#ключевые-концепции-и-паттерны)
5. [Структура проекта](#структура-проекта)
6. [Формат данных](#формат-данных)
7. [Конфигурация](#конфигурация)
8. [Инструкции по созданию нового проекта](#инструкции-по-созданию-нового-проекта)

---

## Назначение документа

Этот документ является **справочным руководством (reference guide)** для AI-агентов при работе с выставочными приложениями-тач-панелями. 

### 🎯 Как использовать этот документ:

**✅ ПРАВИЛЬНО:**
- Изучить архитектурные паттерны и применить их к новой задаче
- Использовать как reference при реализации похожей функциональности
- Заимствовать проверенные решения (idle timer, preloader, navigation flow)
- Следовать технологическому стеку для консистентности между проектами

**❌ НЕПРАВИЛЬНО:**
- Слепо копировать весь код без понимания требований пользователя
- Игнорировать конкретные инструкции разработчика в угоду "шаблону"
- Создавать идентичные проекты, когда требуется другая функциональность

### 📌 Приоритет задач:

1. **Первый приоритет**: требования и задачи пользователя/разработчика
2. **Второй приоритет**: best practices из этого документа (где применимо)
3. **Третий приоритет**: консистентность с существующим проектом

**Запомни**: это template/reference, а не догма. Адаптируй под конкретную задачу!

---

## Технологический стек

### ✅ ОБЯЗАТЕЛЬНЫЕ технологии

#### Core
- **Electron**: `^28.0.0` - desktop приложение с fullscreen kiosk mode
- **React**: `^18.2.0` - UI библиотека
- **TypeScript**: `^5.3.3` - типизация
- **Vite**: `^5.0.8` - сборщик и dev-сервер

#### Build & Development Tools
- **electron-builder**: `^24.9.1` - сборка desktop приложения
- **concurrently**: `^8.2.2` - параллельный запуск dev-сервера и Electron
- **wait-on**: `^7.2.0` - ожидание готовности dev-сервера
- **cross-env**: `^10.1.0` - кросс-платформенные переменные окружения

#### Дополнительные зависимости (по необходимости)
- **cheerio**: `^1.1.2` - парсинг HTML (если требуется)
- **get-video-duration**: `^4.1.0` - расчет длительности видео
- **sharp**: `^0.34.4` - обработка изображений

### 🔧 Конфигурация TypeScript

**tsconfig.json** - strict mode с современными настройками:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true
  }
}
```

### 📦 package.json Scripts

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "electron": "electron .",
    "electron:dev": "concurrently \"npm run dev\" \"wait-on http://localhost:5173 && electron .\"",
    "electron:prod": "npm run build && cross-env NODE_ENV=production electron .",
    "electron:build": "npm run build && electron-builder"
  }
}
```

---

## Архитектура приложения

### 🏗️ Концепция: Kiosk-режим с автовозвратом

**Основные принципы:**

1. **Fullscreen Kiosk Mode** - приложение запускается в полноэкранном режиме без возможности выхода (для выставок)
2. **Автовозврат к заставке** (screensaver) после периода неактивности (90 секунд)
3. **Сенсорное управление** - весь UI адаптирован под touch-события
4. **Предзагрузка ресурсов** - изображения и контент предзагружаются для мгновенного отклика
5. **Видео-заставка** - непрерывный loop видео в режиме ожидания

### 📐 Архитектурные слои

```
┌─────────────────────────────────────────┐
│   Electron Main Process (main.js)      │
│   - Window management                   │
│   - Kiosk configuration                 │
│   - Protocol handlers                   │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│   React Application (App.tsx)           │
│   - View state management               │
│   - Idle timer logic                    │
│   - Navigation orchestration            │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│   Components Layer                      │
│   - VideoIntro (screensaver)            │
│   - MainMenu (navigation)               │
│   - Presentation (slides viewer)        │
│   - VideoModal (video playback)         │
│   - Dashboard charts                    │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│   Custom Hooks Layer                    │
│   - useIdleTimer                        │
│   - useImagePreloader                   │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│   Data Layer (data.json)                │
│   - Static content configuration        │
└─────────────────────────────────────────┘
```

### 🎬 Навигационная схема (State Machine)

```
           [BOOT]
              ↓
        ┌──────────┐
        │  VIDEO   │ ← ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
        │ INTRO    │   (автовозврат 90сек)    │
        └──────────┘                           │
              ↓ (touch/click)                  │
        ┌──────────┐                           │
        │   MENU   │                           │
        └──────────┘                           │
              ↓ (card click)                   │
     ┌────────┼────────┐                       │
     ↓        ↓        ↓        ↓              │
┌─────────┐ ┌────┐ ┌──────┐ ┌────────┐        │
│Dashboards│ │NGFW│ │Products│ │Video  │       │
│         │ │    │ │        │ │Modal  │       │
└─────────┘ └────┘ └──────┘ └────────┘        │
     ↓        ↓        ↓        ↓              │
     └────────┴────────┴────────┴──────────────┘
        (back button / idle timeout)
```

---

## Ключевые концепции и паттерны

### 1️⃣ Idle Timer Pattern (useIdleTimer)

**Назначение**: автоматический возврат к video intro при отсутствии активности

**Реализация**: Custom Hook `useIdleTimer.ts`

```typescript
interface UseIdleTimerOptions {
  timeout: number;        // Таймаут в миллисекундах (90000 = 90 сек)
  onIdle: () => void;     // Callback при срабатывании таймера
  events?: string[];      // События для отслеживания активности
}

// События по умолчанию: 
// ['mousedown', 'mousemove', 'keypress', 'scroll', 'touchstart', 'click']
```

**Использование в App.tsx**:
```typescript
const { resetTimer } = useIdleTimer({
  timeout: 90000,
  onIdle: () => {
    setCurrentView('video');
    setShowVideoModal(false);
  },
  events: ['touchstart', 'click']  // Только touch для киоска
});

// Важно: вызывать resetTimer() при каждом взаимодействии!
```

**Критически важно**: 
- Вызывать `resetTimer()` при любом пользовательском действии
- Для длинных видео - периодически сбрасывать таймер (каждые 30 сек)
- При возврате в menu обязательно сбрасывать таймер

### 2️⃣ Image Preloader Pattern (useImagePreloader)

**Назначение**: предзагрузка изображений презентаций в фоне для мгновенного отображения

**Реализация**: Custom Hook `useImagePreloader.ts`

```typescript
// Использование в MainMenu:
const imagesToPreload = useMemo(() => {
  const images: string[] = [];
  
  // Собираем все thumbnails
  data.ngfwPresentation.slides.forEach(slide => {
    if (slide.thumbnail) images.push(slide.thumbnail);
  });
  
  // Первые слайды (full resolution)
  images.push(data.ngfwPresentation.slides[0].image);
  
  // Background images
  images.push('./ngfw/background.png');
  
  return images;
}, []);

const { isLoading, progress } = useImagePreloader(imagesToPreload);
```

**Важно**: 
- Запускать предзагрузку при монтировании MainMenu
- Предзагружать thumbnails + первые full-size слайды + UI элементы
- Не блокировать UI - предзагрузка в фоне

### 3️⃣ Video Screensaver Pattern (VideoIntro)

**Концепция**: непрерывная циклическая видео-заставка с синхронизацией

**Особенности реализации**:

1. **Плейлист на основе дня недели**:
```typescript
const WEEKDAY_PLAYLIST = [/* будни */];
const WEEKEND_PLAYLIST = [/* выходные */];

function getActivePlaylist() {
  const dayOfWeek = new Date().getDay();
  return (dayOfWeek === 0 || dayOfWeek === 6) 
    ? WEEKEND_PLAYLIST 
    : WEEKDAY_PLAYLIST;
}
```

2. **Синхронизация видео** (бинарный поиск для определения текущего видео):
```typescript
// Вычисляем offset от начала дня
function getDayStartOffsetSeconds(): number {
  const d = new Date();
  return d.getHours() * 3600 + d.getMinutes() * 60 + 
         d.getSeconds() + d.getMilliseconds() / 1000;
}

// Находим текущее видео и позицию через бинарный поиск
function getCurrentVideo(durations: number[], offset: number) {
  // ... binary search implementation
  return { VideoIndex: i, VideoTime: pos };
}
```

3. **Автовосстановление воспроизведения**:
```typescript
// При паузе - автоматически возобновляем
video.addEventListener('pause', () => {
  if (!video.ended) video.play();
});

// При окончании видео - переключаемся на следующее
video.addEventListener('ended', () => {
  const next = getCurrentVideo(durations);
  video.src = playlist[next.VideoIndex].file;
  video.currentTime = next.VideoTime;
  video.play();
});
```

**Параметры**:
- `DEBUG_MODE`: включить debug-панель
- `ENABLE_SYNC`: автоматическая синхронизация (каждые 10 сек)
- `SYNC_INTERVAL_MS`: интервал синхронизации

### 4️⃣ Presentation Pattern (слайд-шоу с навигацией)

**UI структура**:
```
┌────────────────────────────────────────┐
│  Thumbnails    │   Main Slide          │
│  Sidebar       │   Display             │
│                │                       │
│  ▲  (scroll)   │   [Full Image]        │
│  [thumb1]      │                       │
│  [thumb2]*     │   (swipe support)     │
│  [thumb3]      │                       │
│  ▼  (scroll)   │                       │
└────────────────────────────────────────┘
```

**Функционал**:
- Вертикальная прокрутка thumbnails с кнопками ▲/▼
- Клик по thumbnail для перехода к слайду
- Swipe жесты на главном слайде (влево/вправо)
- Автоматический scroll к активному thumbnail

**Реализация swipe**:
```typescript
const handleTouchStart = (e: React.TouchEvent) => {
  touchStartX.current = e.touches[0].clientX;
};

const handleTouchEnd = () => {
  const swipeDistance = touchStartX.current - touchEndX.current;
  const minSwipeDistance = 50;
  
  if (Math.abs(swipeDistance) > minSwipeDistance) {
    if (swipeDistance > 0) handleArrowClick('down');
    else handleArrowClick('up');
  }
};
```

### 5️⃣ Video Modal Pattern

**Назначение**: полноэкранное воспроизведение демо-видео

**Особенности**:
- Автоматический запуск при открытии
- Автоматическое закрытие при окончании видео
- Периодический сброс idle timer (каждые 30 сек) во время воспроизведения
- Закрытие по клику на overlay

```typescript
// Сброс idle timer во время воспроизведения
useEffect(() => {
  const timerResetInterval = setInterval(() => {
    if (video && !video.paused && !video.ended) {
      onResetTimer?.();
    }
  }, 30000); // каждые 30 секунд
  
  return () => clearInterval(timerResetInterval);
}, []);
```

### 6️⃣ Electron Configuration Pattern

**Критически важная конфигурация** (`electron/main.js`):

```javascript
const mainWindow = new BrowserWindow({
  width: 1920,
  height: 1080,
  fullscreen: true,      // Полноэкранный режим
  frame: false,          // Без рамки окна
  kiosk: true,           // Kiosk mode (нельзя выйти)
  autoHideMenuBar: true, // Скрыть меню
  webPreferences: {
    nodeIntegration: false,
    contextIsolation: true,
    preload: path.join(__dirname, 'preload.js'),
    webSecurity: !isDev  // Отключаем только в dev
  }
});

// Отключаем throttling для фоновых окон
app.commandLine.appendSwitch('disable-background-timer-throttling');
app.commandLine.appendSwitch('disable-renderer-backgrounding');

// Dev vs Production loading
if (isDev) {
  mainWindow.loadURL('http://localhost:5173');
  mainWindow.webContents.openDevTools();
} else {
  mainWindow.loadFile(path.join(__dirname, '../dist/index.html'));
}
```

---

## Структура проекта

### 📁 Директория проекта

```
project-name/
├── electron/                    # Electron main process
│   ├── main.js                 # ⚙️ Главный процесс Electron
│   └── preload.js              # 🔌 Preload script (IPC)
│
├── src/                         # React приложение
│   ├── App.tsx                 # 🎯 Главный компонент + state machine
│   ├── App.css                 # 🎨 Стили App
│   ├── main.tsx                # 🚀 Entry point React
│   │
│   ├── components/             # 🧩 Компоненты
│   │   ├── VideoIntro/         # Видео-заставка
│   │   │   ├── VideoIntro.tsx
│   │   │   └── VideoIntro.css
│   │   ├── MainMenu/           # Главное меню
│   │   │   ├── MainMenu.tsx
│   │   │   └── MainMenu.css
│   │   ├── MenuCard/           # Карточка меню
│   │   │   ├── MenuCard.tsx
│   │   │   └── MenuCard.css
│   │   ├── Presentation/       # Слайд-шоу viewer
│   │   │   ├── Presentation.tsx
│   │   │   └── Presentation.css
│   │   ├── VideoModal/         # Модальное видео
│   │   │   ├── VideoModal.tsx
│   │   │   └── VideoModal.css
│   │   └── DashboardCharts/    # Графики (если есть)
│   │       ├── IncidentsChart.tsx
│   │       └── ...
│   │
│   ├── pages/                  # 📄 Страницы-разделы
│   │   ├── Dashboards/
│   │   ├── Products/
│   │   └── PTNgfw/
│   │
│   ├── hooks/                  # 🪝 Custom hooks
│   │   ├── useIdleTimer.ts    # ⏱️ Idle timer
│   │   └── useImagePreloader.ts # 🖼️ Preloader
│   │
│   ├── styles/                 # 🎨 Глобальные стили
│   │   ├── global.css         # Базовые стили
│   │   ├── variables.css      # CSS переменные
│   │   └── fonts.css          # Шрифты
│   │
│   └── assets/                # 📦 Статика
│       ├── fonts/
│       └── images/
│
├── public/                     # 📁 Публичные файлы
│   ├── assets/
│   │   └── videos/            # 🎬 Видео файлы
│   ├── dashboards/            # 📊 Изображения дашбордов
│   ├── products/              # 🛠️ Презентации продуктов
│   │   ├── slides/           # Full-size слайды
│   │   └── thumbnails/       # Превью слайдов
│   └── icons/                 # 🎯 Иконки меню
│
├── data.json                   # 📋 Конфигурация контента
├── index.html                  # 🌐 HTML entry point
├── package.json                # 📦 Зависимости
├── vite.config.ts             # ⚙️ Конфигурация Vite
├── tsconfig.json              # ⚙️ Конфигурация TypeScript
└── README.md                  # 📖 Документация
```

### 🎨 Структура стилей

**Глобальные стили** (`src/styles/global.css`):
```css
@import './variables.css';
@import './fonts.css';

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  overflow: hidden;
  width: 100vw;
  height: 100vh;
}

/* Отключение выделения для киоска */
* {
  -webkit-user-select: none;
  user-select: none;
  -webkit-tap-highlight-color: transparent;
}
```

**CSS переменные** (`src/styles/variables.css`):
```css
:root {
  --color-bg: #000000;
  --color-text-primary: #ffffff;
  --color-accent: #ff0000;
  --font-family: 'CustomFont', Arial, sans-serif;
  /* ... другие переменные */
}
```

---

## Формат данных

### 📋 Структура data.json

**Назначение**: централизованное хранилище всего контента приложения

```json
{
  "cards": [
    {
      "id": 1,
      "title": "Заголовок",
      "subtitle": "Подзаголовок",
      "icon": "icon-filename.png",
      "route": "/dashboards"
    }
  ],
  "dashboards": {
    "title": "Дашборды статистики",
    "tabs": [
      {
        "id": "incidents",
        "label": "Количество инцидентов",
        "subtitle": "Описание графика",
        "data": [
          { "year": 2024, "value": 3813, "growth": 16.0 }
        ]
      }
    ]
  },
  "ngfwPresentation": {
    "title": "PT NGFW",
    "slides": [
      {
        "id": 1,
        "image": "/ngfw/slides/slide-01.png",
        "thumbnail": "/ngfw/thumbnails/slide-01.png"
      }
    ]
  },
  "productsPresentation": {
    "title": "Продукты",
    "slides": [
      {
        "id": 1,
        "image": "/products/slides/slide-01.png",
        "thumbnail": "/products/thumbnails/slide-01.png"
      }
    ]
  }
}
```

**Важные принципы**:
- ✅ Все пути к изображениям - относительные (`./` или `/`)
- ✅ Thumbnails - отдельные оптимизированные изображения (не lazy loading)
- ✅ Структура должна быть flat и легко парсится TypeScript
- ✅ Все тексты хранятся в data.json (не хардкодим в компонентах)

### 🖼️ Формат слайдов

```typescript
interface Slide {
  id: number;
  image: string;         // Путь к full-size изображению
  thumbnail?: string;    // Путь к thumbnail (опционально)
}

interface Presentation {
  title: string;
  slides: Slide[];
}
```

---

## Конфигурация

### ⚙️ vite.config.ts

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: process.env.NODE_ENV === 'production' ? './' : '/',
  build: {
    outDir: 'dist',
    assetsDir: 'assets'
  },
  server: {
    port: 5173
  },
  publicDir: 'public'
});
```

### 📦 electron-builder config (в package.json)

```json
{
  "build": {
    "appId": "com.company.exhibition",
    "productName": "AppName",
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "!dist/**/*.map"
    ],
    "win": {
      "target": ["nsis"],
      "sign": null,
      "signingHashAlgorithms": []
    }
  }
}
```

---

## Инструкции по созданию нового проекта

### 🚀 Пошаговая инструкция для AI-агента

#### Шаг 1: Инициализация проекта

```bash
# Создать директорию проекта
mkdir new-touch-panel
cd new-touch-panel

# Инициализировать npm проект
npm init -y

# Установить ОБЯЗАТЕЛЬНЫЕ зависимости
npm install react react-dom
npm install -D @types/react @types/react-dom
npm install -D @vitejs/plugin-react vite typescript
npm install -D electron electron-builder
npm install -D concurrently wait-on cross-env
```

#### Шаг 2: Создать структуру директорий

```bash
mkdir -p electron src/components src/hooks src/pages src/styles src/assets public/assets/videos public/icons
```

#### Шаг 3: Скопировать базовые файлы конфигурации

1. **package.json** - добавить scripts:
```json
{
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "electron:dev": "concurrently \"npm run dev\" \"wait-on http://localhost:5173 && electron .\"",
    "electron:build": "npm run build && electron-builder"
  }
}
```

2. **tsconfig.json** - скопировать конфигурацию из шаблона
3. **vite.config.ts** - скопировать конфигурацию из шаблона
4. **electron/main.js** - скопировать ПОЛНОСТЬЮ с kiosk настройками

#### Шаг 4: Создать базовые компоненты

**Обязательные компоненты для создания**:
1. ✅ `src/hooks/useIdleTimer.ts` - скопировать полностью
2. ✅ `src/hooks/useImagePreloader.ts` - скопировать полностью
3. ✅ `src/components/VideoIntro/` - адаптировать плейлист видео
4. ✅ `src/components/MainMenu/` - создать структуру меню
5. ✅ `src/components/Presentation/` - скопировать логику слайдов
6. ✅ `src/App.tsx` - создать state machine навигации

#### Шаг 5: Создать data.json

```json
{
  "cards": [
    {
      "id": 1,
      "title": "Раздел 1",
      "subtitle": "Описание раздела",
      "icon": "icon1.png",
      "route": "/section1"
    }
  ],
  "section1Presentation": {
    "title": "Название презентации",
    "slides": [
      {
        "id": 1,
        "image": "/section1/slides/slide-01.png",
        "thumbnail": "/section1/thumbnails/slide-01.png"
      }
    ]
  }
}
```

#### Шаг 6: Настроить глобальные стили

1. Создать `src/styles/variables.css` с брендовыми цветами
2. Создать `src/styles/global.css` с базовыми стилями (отключение selection, overflow: hidden)
3. Импортировать в `main.tsx`

#### Шаг 7: App.tsx - State Machine

**Шаблон навигации**:
```typescript
type ViewType = 'video' | 'menu' | 'section1' | 'section2';

const App: React.FC = () => {
  const [currentView, setCurrentView] = useState<ViewType>('video');
  
  const { resetTimer } = useIdleTimer({
    timeout: 90000,
    onIdle: () => setCurrentView('video'),
    events: ['touchstart', 'click']
  });
  
  const handleVideoClick = useCallback(() => {
    setCurrentView('menu');
    resetTimer();
  }, [resetTimer]);
  
  // ... остальная логика навигации
  
  return (
    <div className="app">
      {currentView === 'video' && (
        <VideoIntro onVideoClick={handleVideoClick} />
      )}
      
      <div style={{ display: currentView === 'menu' ? 'block' : 'none' }}>
        <MainMenu onCardClick={handleCardClick} />
      </div>
      
      {/* Остальные views */}
    </div>
  );
};
```

#### Шаг 8: Тестирование и сборка

```bash
# Dev режим
npm run electron:dev

# Production сборка
npm run electron:build

# Результат в dist-electron/
```

---

## 🎯 Чек-лист для AI-агента

При создании нового проекта убедись, что:

- [ ] Установлены все зависимости из технологического стека
- [ ] Создана корректная структура директорий
- [ ] Скопированы `useIdleTimer` и `useImagePreloader` hooks без изменений
- [ ] Electron main.js настроен в kiosk mode (fullscreen: true, frame: false, kiosk: true)
- [ ] Настроен idle timer на 90 секунд
- [ ] Все компоненты вызывают `resetTimer()` при взаимодействии
- [ ] VideoIntro имеет плейлист видео и логику циклического воспроизведения
- [ ] MainMenu предзагружает изображения презентаций
- [ ] Presentation поддерживает swipe жесты
- [ ] data.json содержит весь контент (без хардкода в компонентах)
- [ ] Глобальные стили отключают user-select и tap-highlight
- [ ] Все пути к ресурсам относительные (работают в production)
- [ ] package.json scripts настроены корректно
- [ ] Разрешение 1920x1080 (fullscreen)

---

## 📝 Дополнительные рекомендации

### Performance
- Использовать `useMemo` для тяжелых вычислений
- Thumbnails оптимизировать до 200-300px
- Видео кодировать в H.264 для лучшей совместимости

### UX для touch
- Кнопки минимум 80x80px
- Увеличенный hit area для кликабельных элементов
- Визуальная обратная связь при нажатии (CSS :active)

### Debug
- Добавить DEBUG_MODE флаг для вывода отладочной информации
- В dev режиме открывать DevTools автоматически

### Безопасность
- contextIsolation: true в Electron
- webSecurity включено в production
- Не использовать nodeIntegration в renderer

---

## ✅ Заключение

Этот документ является **обязательным reference** при создании новых выставочных тач-панелей. Все архитектурные решения проверены в production и обеспечивают:

- Стабильную работу 24/7 в kiosk mode
- Плавный UX без тормозов
- Простоту обновления контента через data.json
- Переиспользование кода между проектами
- Предсказуемость поведения приложения

**Важно**: не отклоняйся от описанных паттернов без веской причины. Консистентность критична для maintainability.
