# 🍎 Apple Touch Panel - Interactive Exhibition App

Интерактивное выставочное приложение на Electron + React + TypeScript с игровой механикой лопания пузырей.

## 📋 Описание проекта

Полноэкранное kiosk-приложение для touch-панелей с интерактивной игрой-аттракцией. Пользователь может лопать анимированные пузыри на экране с визуальными и звуковыми эффектами.

## ✨ Ключевые фичи

- 🫧 **Интерактивные пузыри** - непрерывная генерация пузырей с физикой всплытия
- 🎯 **Tap-to-pop механика** - лопание пузырей по тапу/клику с проверкой коллизий
- 💥 **Эффекты взрыва** - анимированные частицы-брызги при лопании
- 🔊 **Звуковые эффекты** - реалистичный звук лопания пузыря
- 🎨 **Стильный UI** - градиентный фон + продуктовое изображение на переднем плане
- 📱 **Touch-оптимизация** - отключение выделения, tap-highlight, оптимальные размеры
- 🖥️ **Fullscreen kiosk** - полноэкранный режим без возможности выхода

## 🏗️ Архитектура

### Структура проекта

```
Apple/
├── electron/
│   └── main.cjs              # Electron main process (kiosk config)
├── src/
│   ├── components/
│   │   ├── StartScreen/      # Стартовый экран с кнопкой "Начать"
│   │   ├── MenuPage/         # Главная страница с пузырями
│   │   └── Bubbles/          # Компонент игровой механики пузырей
│   ├── styles/
│   │   ├── global.css        # Глобальные стили (reset, touch optimization)
│   │   └── variables.css     # CSS переменные (цвета, шрифты)
│   ├── App.tsx               # Главный компонент (state machine)
│   └── main.tsx              # Entry point
├── public/
│   └── assets/
│       ├── background.png              # Фон стартового экрана
│       ├── bg-gradient.png             # Градиентный фон второй страницы
│       ├── button.png                  # Изображение кнопки "Начать"
│       ├── product-foreground.png      # Продуктовое изображение на переднем плане
│       └── Bubble Pop Shoot! v4.wav    # Звук лопания пузыря
└── package.json
```

### Навигация (State Machine)

```
[START SCREEN]
     ↓ (tap button)
[MENU PAGE with Bubbles Game]
```

## 🎮 Компонент Bubbles - игровая механика

### Основные возможности

1. **Генерация пузырей**
   - Случайная позиция (5-95% ширины экрана)
   - Случайный размер (48-160px)
   - Случайная скорость всплытия (12-28 секунд)
   - Ограничение максимум 40 пузырей одновременно

2. **Физика**
   - CSS animation `floatUp` - движение снизу вверх за пределы экрана
   - Автоматическое удаление пузырей при выходе за границы
   - Плавная анимация появления и исчезновения

3. **Взаимодействие**
   - Обработка `onClick` и touch-событий
   - Анимация лопания (`popAnim`) с эффектом burst ring
   - Генерация 8 частиц-брызг при лопании
   - Звуковой эффект (Audio API)

4. **Визуальные эффекты**
   - Градиентная заливка пузырей (radial-gradient для 3D-эффекта)
   - Box-shadow для объема
   - Анимированные частицы с траекториями разлета
   - Поддержка `prefers-reduced-motion` для accessibility

### Технические детали

```typescript
type Bubble = {
  id: number;
  left: number;      // позиция в % от ширины
  size: number;      // размер в px
  duration: number;  // длительность анимации в секундах
  delay: number;     // задержка перед стартом в секундах
};
```

**Оптимизации:**
- `will-change: transform, opacity` для GPU acceleration
- Ограничение количества пузырей (MAX_BUBBLES = 40)
- Cleanup таймеров при unmount
- Preload audio файла

## 🎨 Дизайн и стили

### Цветовая схема

```css
--color-bg: #E8F1F5;           /* Светло-голубой фон */
--color-text-primary: #2B4A5C; /* Темно-синий текст */
```

### Шрифты

- **Основной**: Georgia, 'Times New Roman', serif (классический с засечками)
- **Fallback**: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto'

### Touch-оптимизация

```css
* {
  -webkit-user-select: none;
  user-select: none;
  -webkit-tap-highlight-color: transparent;
}
```

## 🚀 Технологический стек

### Core

- **Electron** ^28.0.0 - desktop приложение
- **React** ^18.2.0 - UI библиотека
- **TypeScript** ^5.3.3 - типизация
- **Vite** ^5.0.8 - сборщик и dev-сервер

### Build & Development Tools

- **electron-builder** ^24.9.1 - сборка desktop приложения
- **concurrently** ^8.2.2 - параллельный запуск процессов
- **wait-on** ^7.2.0 - ожидание готовности dev-сервера
- **cross-env** ^10.1.0 - кросс-платформенные переменные окружения

## 📦 Установка и запуск

### Установка зависимостей

```bash
npm install
```

### Режим разработки

```bash
npm run electron:dev
```

Запускает Vite dev-сервер на `http://localhost:5173` и Electron приложение с DevTools.

### Production сборка

```bash
npm run electron:build
```

Создает production build в папке `dist-electron/`.

### Отдельные команды

```bash
npm run dev          # Только Vite dev-сервер
npm run build        # TypeScript + Vite build
npm run electron     # Запуск Electron без dev-сервера
npm run electron:prod # Production режим без сборки
```

## ⚙️ Конфигурация

### Electron (electron/main.cjs)

```javascript
{
  width: 1920,
  height: 1080,
  fullscreen: true,
  frame: false,
  kiosk: true,           // Нельзя выйти из приложения
  autoHideMenuBar: true
}
```

### Vite (vite.config.ts)

```typescript
{
  base: process.env.NODE_ENV === 'production' ? './' : '/',
  server: { port: 5173 },
  publicDir: 'public'
}
```

### TypeScript (tsconfig.json)

Strict mode с современными настройками:
- `target: ES2020`
- `jsx: react-jsx`
- `strict: true`
- `moduleResolution: bundler`

## 🎯 Особенности реализации

### 1. Spawning пузырей

Используется рекурсивный `setTimeout` вместо `setInterval` для более гибкого контроля:

```typescript
const spawn = () => {
  // ... генерация пузырей
  const next = random(200, 700); // случайный интервал
  spawnTimer.current = window.setTimeout(() => {
    if (mounted.current) spawn();
  }, next);
};
```

### 2. Звуковые эффекты

```typescript
audioRef.current = new Audio('/assets/Bubble Pop Shoot! v4.wav');
audioRef.current.preload = 'auto';
audioRef.current.volume = 0.6;

// При клике
audio.currentTime = 0; // сброс позиции для быстрого повтора
audio.play().catch(() => {}); // игнорируем ошибки autoplay
```

### 3. Частицы-брызги

Генерируются при лопании пузыря:

```typescript
const angle = random(-Math.PI, Math.PI);
const dist = random(12, 60);
const dx = Math.cos(angle) * dist;
const dy = Math.sin(angle) * dist - random(6, 20); // вверх с гравитацией
```

### 4. Layering (z-index)

```
z-index: 0     - Градиентный фон (bg-gradient.png)
z-index: 1     - Продуктовое изображение (product-foreground.png)
z-index: 99999 - Пузыри (Bubbles root)
z-index: 100000 - Частицы-брызги
```

### 5. Performance

- GPU acceleration: `will-change: transform, opacity`
- Ограничение пузырей: `MAX_BUBBLES = 40`
- Cleanup: удаление пузырей после выхода за границы
- Reduced motion: `@media (prefers-reduced-motion: reduce)`

## 🐛 Debug и разработка

### DevTools

В dev режиме автоматически открываются Chrome DevTools:

```javascript
mainWindow.webContents.openDevTools();
```

### Console logging

В компоненте `Bubbles.tsx` есть debug логи:

```typescript
console.debug('bubble popped', id);
console.debug('spawn particles', parts);
```

### Hot Module Replacement (HMR)

Vite автоматически перезагружает компоненты при изменении файлов.

## 📱 Touch-панель рекомендации

### Оптимальные параметры

- **Разрешение**: 1920x1080 (16:9)
- **Размер пузырей**: 48-160px (комфортно для тача)
- **Звук**: убедись что volume настроен правильно (default 0.6)
- **Освещение**: учитывай блики на глянцевых экранах

### Калибровка

Если пузыри слишком быстро/медленно двигаются, измени параметры в `Bubbles.tsx`:

```typescript
const duration = Number(random(12, 28).toFixed(2)); // увеличь/уменьши диапазон
```

## 🔧 Возможные доработки

### Идеи для улучшения

1. **Счётчик очков** - отображать количество лопнутых пузырей
2. **Комбо-система** - бонусы за быстрое лопание подряд
3. **Уровни сложности** - больше/меньше пузырей, разная скорость
4. **Idle timer** - автовозврат на стартовый экран через N секунд неактивности
5. **Мультитач** - одновременное лопание нескольких пузырей
6. **Анимация кнопки "Начать"** - пульсация для привлечения внимания
7. **Фоновая музыка** - ненавязчивый ambient трек

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

Проект разработан как референсный пример выставочной touch-панели с игровой механикой.

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025-11-21
